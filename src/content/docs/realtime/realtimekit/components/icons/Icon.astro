---
import { z } from "astro:schema";
import { Icon } from "./Icon";

const props = z.object({
	name: z.string(),
	className: z.string().optional(),
});

const { name, className } = props.parse(Astro.props);
---

<Icon client:load name={name} className={className} />
